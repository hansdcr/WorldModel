# 迭代 1 - 修复记录

> **问题**: matplotlib 中文显示为方框（乱码）
> **修复日期**: 2025-11-21
> **影响范围**: `spatial_concept.py`, `spatial_relations.py`

---

## 🐛 问题描述

### 症状
运行迭代 1 的代码时，3D 图形中的中文标签显示为方框（□□□），控制台输出警告：

```
UserWarning: Glyph 36724 (\N{CJK UNIFIED IDEOGRAPH-8F74}) missing from font(s) DejaVu Sans.
```

### 原因
matplotlib 默认使用 DejaVu Sans 字体，该字体不包含中文字符。

---

## ✅ 解决方案

### 修复方法
在代码开头添加中文字体配置：

```python
# 配置中文字体支持（macOS）
import matplotlib.pyplot as plt
plt.rcParams['font.sans-serif'] = ['Arial Unicode MS']  # macOS 系统自带
plt.rcParams['axes.unicode_minus'] = False  # 解决负号显示问题
```

### 修改的文件

1. **`code/iteration_01/spatial_concept.py`**
   - 在导入语句后添加字体配置（第 17-19 行）

2. **`code/iteration_01/spatial_relations.py`**
   - 在导入语句后添加字体配置（第 17-19 行）

---

## 🔧 详细修改

### 修改前
```python
import numpy as np
import matplotlib.pyplot as plt
from mpl_toolkits.mplot3d import Axes3D


def visualize_3d_point(point, label="Point"):
    # ... 代码
```

### 修改后
```python
import numpy as np
import matplotlib.pyplot as plt
from mpl_toolkits.mplot3d import Axes3D

# 配置中文字体支持（macOS）
plt.rcParams['font.sans-serif'] = ['Arial Unicode MS']  # macOS 系统自带
plt.rcParams['axes.unicode_minus'] = False  # 解决负号显示问题


def visualize_3d_point(point, label="Point"):
    # ... 代码
```

---

## 🎯 验证方法

### 测试步骤
```bash
# 1. 激活虚拟环境
source venv/bin/activate

# 2. 运行修复后的代码
python code/iteration_01/spatial_concept.py

# 3. 检查 3D 图形
# - 标题应该正确显示 "3D 空间中的点"
# - 坐标轴标签应该显示中文
# - 点的标签应该显示 "沙发中心"
```

### 预期结果
- ✅ 所有中文字符正常显示
- ✅ 无字体警告
- ✅ 图形清晰可读

---

## 📝 不同操作系统的字体配置

### macOS
```python
plt.rcParams['font.sans-serif'] = ['Arial Unicode MS']
```

### Windows
```python
plt.rcParams['font.sans-serif'] = ['SimHei']  # 黑体
# 或
plt.rcParams['font.sans-serif'] = ['Microsoft YaHei']  # 微软雅黑
```

### Linux
```python
plt.rcParams['font.sans-serif'] = ['WenQuanYi Micro Hei']  # 文泉驿微米黑
# 或
plt.rcParams['font.sans-serif'] = ['Noto Sans CJK']
```

### 通用解决方案（查找系统可用字体）
```python
import matplotlib.font_manager as fm

# 列出所有可用字体
fonts = [f.name for f in fm.fontManager.ttflist]
chinese_fonts = [f for f in fonts if 'Hei' in f or 'Song' in f or 'Arial' in f]
print("可用的中文字体：", chinese_fonts)

# 选择第一个可用的中文字体
if chinese_fonts:
    plt.rcParams['font.sans-serif'] = [chinese_fonts[0]]
```

---

## 💡 经验总结

### 1. 为什么会出现这个问题？
- matplotlib 是国际化工具，默认不支持中文
- 需要手动配置支持中文的字体
- 不同操作系统的字体名称不同

### 2. 如何避免类似问题？
在后续迭代中：
- **模板化**: 创建一个 `utils/visualization.py` 工具模块
- **统一配置**: 在一个地方配置字体，所有代码引用
- **跨平台**: 自动检测操作系统并选择合适的字体

### 3. 最佳实践
```python
# 创建 utils/matplotlib_config.py
import platform
import matplotlib.pyplot as plt

def setup_chinese_font():
    """配置 matplotlib 的中文字体支持"""
    system = platform.system()

    if system == 'Darwin':  # macOS
        plt.rcParams['font.sans-serif'] = ['Arial Unicode MS']
    elif system == 'Windows':
        plt.rcParams['font.sans-serif'] = ['SimHei']
    else:  # Linux
        plt.rcParams['font.sans-serif'] = ['WenQuanYi Micro Hei']

    plt.rcParams['axes.unicode_minus'] = False
    print(f"✅ 已配置 {system} 系统的中文字体")

# 使用方式
from utils.matplotlib_config import setup_chinese_font
setup_chinese_font()
```

---

## 🔄 后续改进

### 计划在迭代 5 完成时：
- [ ] 创建 `utils/matplotlib_config.py` 工具模块
- [ ] 自动检测操作系统并配置字体
- [ ] 更新所有现有代码使用统一配置
- [ ] 在 README.md 中添加字体配置说明

---

## 📊 影响评估

### 影响范围
- **已修复**: 迭代 1 的 2 个 Python 文件
- **待处理**: 迭代 2-45 的新文件（将使用统一配置）

### 用户影响
- **修复前**: 图形中文显示为方框，影响理解
- **修复后**: 中文正常显示，用户体验良好

---

## ✅ 修复检查清单

- [x] 识别问题原因
- [x] 找到解决方案（字体配置）
- [x] 修改 `spatial_concept.py`
- [x] 修改 `spatial_relations.py`
- [x] 测试修复效果
- [x] 文档化修复过程
- [x] 计划后续改进
- [ ] 创建通用工具模块（迭代 5）

---

## 🎓 学习收获

### 技术知识
1. **matplotlib 字体系统**: 理解了 matplotlib 如何处理字体
2. **跨平台开发**: 了解了不同系统的字体差异
3. **配置管理**: 学会了集中管理配置的重要性

### 开发经验
1. **问题定位**: 从警告信息快速定位问题
2. **解决方案**: 查找官方文档和社区方案
3. **预防措施**: 提前规划通用解决方案

---

**修复人**: Claude AI
**复核人**: ______
**生效时间**: 2025-11-21

---

**下一步**: 继续学习迭代 1，运行修复后的代码，验证中文显示正常。
