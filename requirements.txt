# ============================================
# 世界模型学习项目 - Python 依赖配置
# 版本: v2.0 (Marble Edition)
# 创建日期: 2025-11-21
# ============================================

# ============================================
# 核心深度学习框架
# ============================================
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# ============================================
# 多模态处理 (Phase 1: 迭代 7-14)
# ============================================
transformers>=4.30.0          # Hugging Face 模型库 (CLIP, BERT, T5)
diffusers>=0.21.0             # 扩散模型库 (Stable Diffusion)
open-clip-torch>=2.20.0       # OpenCLIP 实现
sentence-transformers>=2.2.0  # 文本编码器

# ============================================
# 3D 处理 (Phase 2: 迭代 15-22)
# ============================================
# Gaussian Splatting (Marble 核心技术)
# 注意: gsplat 需要 CUDA，如果没有 GPU 可以先跳过
# gsplat>=0.1.0                # 暂时注释，等需要时再安装

# 3D 深度学习与处理
pytorch3d>=0.7.5              # Facebook 3D 深度学习库
open3d>=0.17.0                # 点云与网格处理
trimesh>=3.23.0               # 网格操作
pymeshlab>=2022.2             # 网格处理工具

# NeRF 相关 (作为对比学习)
# nerfstudio>=0.3.0            # 暂时注释，等需要时再安装

# ============================================
# 计算机视觉 (Phase 0-1: 迭代 1-14)
# ============================================
opencv-python>=4.8.0          # 图像处理
opencv-contrib-python>=4.8.0  # OpenCV 扩展
Pillow>=10.0.0                # 图像操作
imageio>=2.31.0               # 图像/视频读写
imageio-ffmpeg>=0.4.9         # 视频编解码
scikit-image>=0.21.0          # 图像处理算法
albumentations>=1.3.0         # 数据增强

# ============================================
# 科学计算基础
# ============================================
numpy>=1.24.0
scipy>=1.11.0
scikit-learn>=1.3.0
pandas>=2.0.0

# ============================================
# 可视化 (所有阶段)
# ============================================
matplotlib>=3.7.0             # 2D 可视化
plotly>=5.15.0                # 3D 交互可视化
seaborn>=0.12.0               # 统计可视化
tqdm>=4.65.0                  # 进度条

# ============================================
# 实验管理与日志
# ============================================
wandb>=0.15.0                 # 实验跟踪（可选）
tensorboard>=2.13.0           # TensorBoard 可视化
jupyter>=1.0.0                # Jupyter Notebook
ipywidgets>=8.0.0             # Jupyter 交互组件

# ============================================
# 工具库
# ============================================
einops>=0.6.1                 # 张量操作简化
timm>=0.9.0                   # PyTorch 图像模型库
huggingface-hub>=0.16.0       # Hugging Face Hub 下载
safetensors>=0.3.0            # 安全的模型序列化

# ============================================
# 开发工具
# ============================================
pytest>=7.4.0                 # 单元测试
black>=23.7.0                 # 代码格式化
flake8>=6.1.0                 # 代码检查
ipdb>=0.13.0                  # 调试工具

# ============================================
# 可选：加速库 (有 NVIDIA GPU 时)
# ============================================
# ninja>=1.11.0                # 编译加速
# xformers>=0.0.20             # 高效 Transformer 实现

# ============================================
# 安装说明
# ============================================
# 基础安装（CPU版本）:
#   pip install -r requirements.txt
#
# GPU 版本（推荐）:
#   1. 先安装 PyTorch with CUDA:
#      pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
#   2. 再安装其他依赖:
#      pip install -r requirements.txt
#
# 完整版本（包含所有可选项）:
#   取消注释相关行后安装
#
# 分阶段安装策略（推荐给初学者）:
#   - 迭代 1-6:  只需要 numpy, matplotlib, opencv, open3d
#   - 迭代 7-14: 添加 transformers, diffusers, clip
#   - 迭代 15-22: 添加 pytorch3d, trimesh, gsplat
#   - 迭代 23+:  添加其他高级库
